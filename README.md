## 💻 OSPranto Tech: GitHub থেকে APK তৈরি ও ব্লগিং প্রক্রিয়া

এই স্ক্রিপ্টটি আপনাকে আপনার Android প্রজেক্টটি GitHub-এ আপলোড করতে এবং GitHub Actions ব্যবহার করে স্বয়ংক্রিয়ভাবে APK তৈরি করতে সাহায্য করবে।

## 🌐 Chrome ব্রাউজার ব্যবহার করে GitHub-এ ফাইল আপলোড করার ম্যানুয়াল প্রক্রিয়া

এখানে `git init`, `git add`, `git commit`, `git push` কমান্ডগুলোর বদলে ব্রাউজারের UI ব্যবহার করে কিভাবে কাজ করবেন, তার ধাপে ধাপে নির্দেশিকা দেওয়া হলো:

### ধাপ ১: GitHub-এ নতুন রিপোজিটোরি তৈরি

১.  Chrome ব্রাউজারে **GitHub-এ লগইন** করুন।
২.  ডান দিকে উপরে আপনার প্রোফাইল ছবিতে ক্লিক করে অথবা বাম দিকের সাইডবারে **`New`** বাটনে ক্লিক করে একটি **নতুন রিপোজিটোরি** তৈরি করুন।
৩.  **Repository name** দিন (যেমন: `OSPranto-Android-App`)।
৪.  `Description` যোগ করুন (ঐচ্ছিক)।
৫.  এটি **Public** রাখবেন, যাতে সবাই আপনার কোড দেখতে পারে।
৬.  `Initialize this repository with:` এর নিচে থাকা অপশনগুলো **অনির্বাচন করে রাখুন** (কোনো `README`, `gitignore`, বা `License` এখন যোগ করার দরকার নেই)।
৭.  **`Create repository`** সবুজ বাটনে ক্লিক করুন।

### ধাপ ২: ফাইলগুলো রিপোজিটোরিতে আপলোড করা

এখন আপনি আপনার লোকাল Android প্রজেক্টের সব ফাইল এই নতুন তৈরি হওয়া রিপোজিটোরিতে আপলোড করবেন।

১.  নতুন তৈরি হওয়া রিপোজিটোরি পেজে (এটি ফাঁকা দেখাবে) যান।
২.  আপনি দুটি অপশন দেখতে পাবেন: **`uploading an existing file`** এবং **`command line`**।
৩.  **`uploading an existing file`** লিঙ্কে ক্লিক করুন।

৪.  **আপলোড পেজে:**
    * আপনার লোকাল Android প্রজেক্ট ফোল্ডারে যান (যেখানে `app`, `build.gradle`, `gradlew` ইত্যাদি ফাইলগুলো আছে)।
    * **`.git` এবং `build` ফোল্ডার ছাড়া** সব ফাইল ও ফোল্ডার (**`app`**, **`gradle`**, **`build.gradle`**, **`gradlew`** ইত্যাদি) নির্বাচন করুন।
    * এগুলো ড্র্যাগ করে GitHub-এর আপলোড এরিয়াতে ছেড়ে দিন (বা **`choose your files`** বাটনে ক্লিক করে নির্বাচন করুন)।

৫.  **আপলোড প্রক্রিয়া:**
    * GitHub স্বয়ংক্রিয়ভাবে ফাইলগুলো আপলোড করা শুরু করবে।
    * আপলোড শেষ হলে, নিচে **"Commit changes"** সেকশনে যান।
    * কমিট মেসেজ বক্সে লিখুন: **`Initial commit: OSPranto Tech Android Project files`** (এটি `git commit -m "..."` কমান্ডের কাজ করবে)।

৬.  সবুজ **`Commit changes`** বাটনে ক্লিক করুন।



### ধাপ ৩ : GitHub Actions কনফিগারেশন যোগ করা (অনলাইন/ওয়েব)

এই ধাপে আমরা আপনার রিপোজিটোরিতে সেই স্ক্রিপ্টটি যোগ করব, যা APK তৈরির কাজটি করবে।

১.  আপনার GitHub রিপোজিটোরিতে যান।
২.  **"Add file"** \> **"Create new file"** এ ক্লিক করুন।
৩.  ফাইলের নাম দিন: `.github/workflows/android_build.yml`

৪.  নিচের **`android_build.yml` স্ক্রিপ্টটি** কপি করে সেখানে পেস্ট করুন। এটি একটি **Debug APK** তৈরি করবে, যা পরীক্ষার জন্য যথেষ্ট।

#### 📜 `android_build.yml` (ডিবাগ বিল্ড)

```yaml
# 📂 .github/workflows/android_build.yml

name: OSPranto Tech - Auto Android Debug Build

# 'main' ব্রাঞ্চে কোড পুশ করলেই এই ওয়ার্কফ্লো রান হবে
on:
  push:
    branches: [ main ]
  workflow_dispatch: # ম্যানুয়ালি রান করার জন্য

jobs:
  build:
    runs-on: ubuntu-latest # বিল্ড চালানোর জন্য OS

    steps:
    - name: ⬇️ কোড চেকআউট (Checkout Code)
      uses: actions/checkout@v4
      
    - name: ☕ জাভা সেটআপ (Setup Java)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17' # Android ডেভেলপমেন্টের জন্য স্ট্যান্ডার্ড জাভা ভার্সন

    - name: 📱 অ্যান্ড্রয়েড SDK সেটআপ (Setup Android SDK)
      # স্বয়ংক্রিয়ভাবে অ্যান্ড্রয়েড SDK ও পরিবেশ সেটআপ করে
      uses: android-actions/setup-android@v3

    - name: 🛠️ Gradle এজেকিউটেবল পারমিশন দিন (Make gradlew executable)
      run: chmod +x gradlew

    - name: 🏗️ অ্যাপ বিল্ড করুন (Build Debug APK)
      # এটি ডেবাগ ভার্সনের APK তৈরি করবে
      run: ./gradlew assembleDebug

    - name: 📦 APK ফাইল খুঁজুন (Find APK file path)
      # তৈরি হওয়া .apk ফাইলটি খুঁজে বের করুন
      run: echo "APK_PATH=$(find app/build/outputs/apk/debug -name 'app-debug.apk' | head -n 1)" >> $GITHUB_ENV
      
    - name: ⬆️ বিল্ড আউটপুট আপলোড করুন (Upload APK Artifact)
      uses: actions/upload-artifact@v4
      with:
        name: OSPranto-Tech-Debug-APK
        path: ${{ env.APK_PATH }}
        retention-days: 7 # APK টি ৭ দিনের জন্য ডাউনলোডযোগ্য থাকবে
```

৫.  ফাইলটি কমিট করুন। সাথে সাথে আপনার বিল্ড প্রক্রিয়া শুরু হয়ে যাবে।

### ধাপ ৪ : APK ডাউনলোড করা

১.  আপনার GitHub রিপোজিটোরিতে **`Actions`** ট্যাবে যান।
২.  দেখুন আপনার `OSPranto Tech - Auto Android Debug Build` ওয়ার্কফ্লোটি সফলভাবে শেষ হয়েছে।
৩.  সফল বিল্ডটির উপর ক্লিক করে **Run Details** পেজে যান।
৪.  পেজের নিচে **Artifacts** সেকশনে যান।
৫.  **`OSPranto-Tech-Debug-APK`** আর্টফ্যাক্টটি ডাউনলোড করুন। এটি একটি জিপ ফাইল হবে।
৬.  জিপ ফাইলটি এক্সট্র্যাক্ট করলে আপনি **`app-debug.apk`** ফাইলটি পেয়ে যাবেন।

### ধাপ ৪: APK ফাইলটি Blogger সাইটে আপলোড করা

Blogger সরাসরি APK ফাইল হোস্ট করতে দেয় না। তাই আপনাকে ফাইলটি অন্য কোনো হোস্টিং প্ল্যাটফর্মে আপলোড করে তার ডাউনলোড লিঙ্কটি Blogger-এ দিতে হবে।

#### ☁️ ক্লাউড স্টোরেজ ব্যবহার করুন:

১.  **Google Drive / MediaFire / DropBox** এর মতো একটি ক্লাউড স্টোরেজ সার্ভিসে আপনার ডাউনলোড করা **`app-debug.apk`** ফাইলটি আপলোড করুন।
২.  আপলোড করার পর, ফাইলটির **পাবলিক শেয়ারিং লিঙ্ক** তৈরি করুন।
৩.  আপনার Blogger পোস্টে গিয়ে সুন্দর করে একটি পোস্ট লিখুন (ব্র্যান্ডিং: **"OSPranto Tech-এর নতুন অ্যাপ রিলিজ\!"**)।
৪.  ডাউনলোড বাটন তৈরি করে সেখানে ক্লাউড স্টোরেজ থেকে পাওয়া **ডাউনলোড লিঙ্কটি** যুক্ত করুন।

```html
<a href="[আপনার Google Drive/MediaFire ডাউনলোড লিঙ্ক]" style="background-color: #4CAF50; color: white; padding: 15px 25px; text-align: center; text-decoration: none; display: inline-block; border-radius: 8px; font-weight: bold;">
  ⬇️ ডাউনলোড করুন (OSPranto Tech APK)
</a>
```
